server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # API 프록시 (백엔드 호출 시 필요하면)
    location /api/ {
        proxy_pass http://3.36.110.234:8080/;
    }
}